# 16 - Ajuste de logos redirecionando para home

## Ação realizada

- Ajustado `TopBar` para tornar a área da logo/título clicável com destino em `/`.
- Ajustado a logo em `/onboarding/workspace` para ser clicável com destino em `/`.
- Mantido o fluxo de criação e seleção de workspace redirecionando para `/dashboard`.

## Resultado

- O comportamento de navegação ficou concentrado nas logos, com retorno para home (`/`) quando clicadas.
